import { ProfessionFilterPipe } from './profession-filter.pipe';

describe('ProfessionFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ProfessionFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
