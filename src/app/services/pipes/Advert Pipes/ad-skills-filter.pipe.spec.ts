import { AdSkillsFilterPipe } from './ad-skills-filter.pipe';

describe('AdSkillsFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new AdSkillsFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
