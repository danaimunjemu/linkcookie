import { JobTitleFilterPipe } from './job-title-filter.pipe';

describe('JobTitleFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new JobTitleFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
