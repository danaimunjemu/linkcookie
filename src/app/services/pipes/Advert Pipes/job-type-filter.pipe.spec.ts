import { JobTypeFilterPipe } from './job-type-filter.pipe';

describe('JobTypeFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new JobTypeFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
