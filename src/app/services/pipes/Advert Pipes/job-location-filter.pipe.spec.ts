import { JobLocationFilterPipe } from './job-location-filter.pipe';

describe('JobLocationFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new JobLocationFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
