import { IndustryFilterPipe } from './industry-filter.pipe';

describe('IndustryFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new IndustryFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
